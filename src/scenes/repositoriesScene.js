import React from 'react';
import RepositoriesContainer from '../containers/repositories/repositoriesContainer'

const RepositoriesScene = props => (
  <RepositoriesContainer navigation={props.navigation}/>
)

export default RepositoriesScene;
