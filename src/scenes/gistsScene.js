import React from 'react';
import GistsContainer from '../containers/gists/gistsContainer'

const GistsScene = props => (
  <GistsContainer navigation={props.navigation}/>
)

export default GistsScene;
